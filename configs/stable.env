# stable.env — loaded for build_type=stable
# Add CONFIG overrides or env vars that should apply to all stable builds
# Format: KEY=VALUE (written to GITHUB_ENV, not defconfig)

# Example: force LTO thin on stable
# LTO=thin

# No experimental changes in stable — intentionally minimal
