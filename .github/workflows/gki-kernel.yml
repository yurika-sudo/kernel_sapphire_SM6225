name: Build GKI Kernel

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'

permissions:
  contents: write

env:
  KERNEL_BRANCH: android13-5.15-lts
  CLANG_VERSION: r547379
  DEVICE: sapphiren

jobs:
  build-kernels:
    name: "Build ${{ matrix.variant.name }}"
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        variant:
          - name: "GKI-KSU"
            output: "Image.ksu"
            enable_ksu: true
          - name: "GKI-NoKSU"
            output: "Image.noksu"
            enable_ksu: false
    
    steps:
      - name: Setup environment
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo apt-get update -qq && sudo apt-get install -y \
            bc bison flex libssl-dev libelf-dev build-essential \
            zip git curl aria2 repo jq
      
      - name: Download clang
        run: |
          mkdir -p prebuilts/clang/host/linux-x86
          aria2c -x16 -s16 -o clang.tar.gz \
            "https://github.com/topnotchfreaks/clang/releases/download/v1.0.0/clang-${{ env.CLANG_VERSION }}.tar.gz"
          mkdir -p prebuilts/clang/host/linux-x86/clang-${{ env.CLANG_VERSION }}
          tar -xzf clang.tar.gz -C prebuilts/clang/host/linux-x86/clang-${{ env.CLANG_VERSION }}
          rm clang.tar.gz
      
      - name: Initialize kernel source
        run: |
          repo init -u https://android.googlesource.com/kernel/manifest \
            -b common-${{ env.KERNEL_BRANCH }} --depth=1
          repo sync -c -j$(nproc) --no-tags --no-clone-bundle
          
          rm -rf prebuilts/clang/host/linux-x86/clang-{3289846,r450784e,stable}
          
          cd common
          git checkout -f && git clean -fdx
          
          sed -i -e 's/^BRANCH=.*/BRANCH=android13-5.15/' \
                 -e 's/^CLANG_VERSION=.*/CLANG_VERSION=${{ env.CLANG_VERSION }}/' \
                 build.config.constants
          
          # Disable ALL ABI checks completely
          sed -i 's/^ABI_DEFINITION=/#ABI_DEFINITION=/g' build.config.gki.aarch64
          sed -i 's/^KMI_SYMBOL_LIST=/#KMI_SYMBOL_LIST=/g' build.config.gki.aarch64
          sed -i 's/^ADDITIONAL_KMI_SYMBOL_LISTS=/#ADDITIONAL_KMI_SYMBOL_LISTS=/g' build.config.gki.aarch64
          sed -i 's/^TRIM_NONLISTED_KMI=/#TRIM_NONLISTED_KMI=/g' build.config.gki.aarch64
          sed -i 's/^GKI_MODULES_LIST=/#GKI_MODULES_LIST=/g' build.config.gki.aarch64
          sed -i 's/^GKI_BUILD_CONFIG_FRAGMENT=/#GKI_BUILD_CONFIG_FRAGMENT=/g' build.config.gki.aarch64
          sed -i '/^POST_DEFCONFIG_CMDS=/d' build.config.gki
          
          # Add skip flags
          echo '' >> build.config.gki.aarch64
          echo 'SKIP_ABI_CHECKS=1' >> build.config.gki.aarch64
          echo 'SKIP_CP_KERNEL_HDR=1' >> build.config.gki.aarch64
          echo 'ABI_DEFINITION=' >> build.config.gki.aarch64
          echo 'KMI_SYMBOL_LIST=' >> build.config.gki.aarch64
          echo 'ADDITIONAL_KMI_SYMBOL_LISTS=' >> build.config.gki.aarch64
          
          # Create empty ABI files to avoid "file not found" errors
          mkdir -p android
          touch android/abi_gki_aarch64
          touch android/abi_gki_aarch64_virtual_device
          touch android/abi_gki_aarch64_db845c
          touch android/abi_symbollist.raw
          
          cd ..
      
      - name: Setup Wild-KSU
        if: matrix.variant.enable_ksu
        working-directory: common
        run: |
          curl -LSs "https://raw.githubusercontent.com/WildKernels/Wild_KSU/wild/kernel/setup.sh" | bash -s stable
          [ -d "Wild_KSU" ] && ln -sf Wild_KSU drivers/kernelsu || exit 1
          
          git clone -q --depth=1 https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android13-5.15
          patch -p1 < susfs4ksu/kernel_patches/50_add_susfs_in_gki-android13-5.15.patch 2>/dev/null || true
          [ -f "susfs4ksu/kernel_patches/fs/susfs.c" ] && cp susfs4ksu/kernel_patches/fs/susfs.c fs/
          cp susfs4ksu/kernel_patches/include/linux/susfs*.h include/linux/ 2>/dev/null || true
          
          if [ -f "Wild_KSU/kernel/ksu.c" ]; then
            grep -q "susfs.h" Wild_KSU/kernel/ksu.c || \
              sed -i '/#include <linux\/fs.h>/a #include <linux/susfs.h>' Wild_KSU/kernel/ksu.c
            grep -q "susfs_init" Wild_KSU/kernel/ksu.c || \
              sed -i '/ksu_core_init();/a \\tsusfs_init();' Wild_KSU/kernel/ksu.c
          fi
          rm -rf susfs4ksu
      
      - name: Setup Baseband Guard
        if: matrix.variant.enable_ksu
        working-directory: common
        run: |
          git clone -q --depth=1 https://github.com/vc-teahouse/Baseband-guard.git
          ln -sf ../Baseband-guard security/baseband-guard
          grep -q "baseband-guard" security/Makefile || \
            echo 'obj-$(CONFIG_BBG) += baseband-guard/' >> security/Makefile
          grep -q "baseband-guard/Kconfig" security/Kconfig || \
            sed -i '/source "security\/keys\/Kconfig"/a source "security/baseband-guard/Kconfig"' security/Kconfig
          sed -i '/^config LSM$/,/^help$/{ /default/ { /baseband_guard/! s/selinux/selinux,baseband_guard/ } }' security/Kconfig
      
      - name: Configure kernel (KSU)
        if: matrix.variant.enable_ksu
        working-directory: common
        run: |
          cat >> "arch/arm64/configs/gki_defconfig" << 'EOF'
          CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y
          CONFIG_KALLSYMS=y
          CONFIG_TCP_CONG_ADVANCED=y
          CONFIG_TCP_CONG_BIC=n
          CONFIG_TCP_CONG_WESTWOOD=y
          CONFIG_TCP_CONG_HTCP=n
          CONFIG_TCP_CONG_BBR=y
          CONFIG_DEFAULT_WESTWOOD=y
          CONFIG_DEFAULT_TCP_CONG="westwood"
          CONFIG_OVERLAY_FS=y
          CONFIG_TMPFS_XATTR=y
          CONFIG_TMPFS_POSIX_ACL=y
          CONFIG_F2FS_FS=y
          CONFIG_F2FS_FS_SECURITY=y
          CONFIG_F2FS_FS_ENCRYPTION=y
          CONFIG_F2FS_FAULT_INJECTION=n
          CONFIG_F2FS_STAT_FS=y
          CONFIG_ZRAM=y
          CONFIG_ZRAM_DEF_COMP_LZ4=y
          CONFIG_ZRAM_DEF_COMP="lz4"
          CONFIG_ZRAM_MEMORY_TRACKING=n
          CONFIG_MEMCG=y
          CONFIG_MEMCG_SWAP=y
          CONFIG_LRU_GEN=y
          CONFIG_SCHED_AUTOGROUP=y
          CONFIG_SCHED_TUNE=y
          CONFIG_ENERGY_MODEL=y
          CONFIG_COMPACTION=y
          CONFIG_PAGE_COUNTER=y
          CONFIG_PSI=n
          CONFIG_PSI_DEFAULT_DISABLED=y
          CONFIG_MODULE_COMPRESS_NONE=y
          CONFIG_MODULE_SIG=n
          CONFIG_MODULE_FORCE_LOAD=n
          CONFIG_NET_SCHED=y
          CONFIG_NET_SCH_FQ_CODEL=y
          CONFIG_NET_SCH_FQ=y
          CONFIG_TCP_BBR2=y
          CONFIG_NET_NS=y
          CONFIG_HZ_1000=y
          CONFIG_HZ=1000
          CONFIG_KSU=y
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          CONFIG_KPROBE_EVENTS=y
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
          CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_SU=y
          CONFIG_BBG=y
          CONFIG_KPM=y
          # CONFIG_DEBUG_KERNEL is not set
          # CONFIG_TRACING_SUPPORT is not set
          # CONFIG_TRACING is not set
          # CONFIG_FTRACE is not set
          # CONFIG_TRACEPOINTS is not set
          # CONFIG_FUNCTION_TRACER is not set
          # CONFIG_FUNCTION_GRAPH_TRACER is not set
          # CONFIG_TRACE_PRINTK is not set
          # CONFIG_DYNAMIC_FTRACE is not set
          # CONFIG_DEBUG_INFO is not set
          # CONFIG_DEBUG_INFO_DWARF_TOOLCHAIN_DEFAULT is not set
          # CONFIG_DEBUG_INFO_DWARF4 is not set
          # CONFIG_DEBUG_INFO_DWARF5 is not set
          # CONFIG_DEBUG_INFO_BTF is not set
          # CONFIG_DEBUG_INFO_REDUCED is not set
          # CONFIG_DEBUG_INFO_COMPRESSED is not set
          # CONFIG_DEBUG_FS is not set
          # CONFIG_DYNAMIC_DEBUG is not set
          # CONFIG_DEBUG_PAGEALLOC is not set
          # CONFIG_PAGE_OWNER is not set
          # CONFIG_PAGE_POISONING is not set
          # CONFIG_SLUB_DEBUG is not set
          # CONFIG_SLUB_DEBUG_ON is not set
          # CONFIG_DEBUG_OBJECTS is not set
          # CONFIG_DEBUG_OBJECTS_TIMERS is not set
          # CONFIG_PROVE_LOCKING is not set
          # CONFIG_DEBUG_LOCKDEP is not set
          # CONFIG_DEBUG_SPINLOCK is not set
          # CONFIG_DEBUG_MUTEXES is not set
          # CONFIG_LOCK_STAT is not set
          # CONFIG_STACKTRACE is not set
          # CONFIG_DEBUG_STACK_USAGE is not set
          # CONFIG_DEBUG_STACKOVERFLOW is not set
          # CONFIG_RCU_TRACE is not set
          # CONFIG_RCU_EQS_DEBUG is not set
          # CONFIG_PRINTK_TIME is not set
          # CONFIG_PRINTK_CALLER is not set
          # CONFIG_PRINTK_INDEX is not set
          EOF
      
      - name: Configure kernel (NoKSU)
        if: ${{ !matrix.variant.enable_ksu }}
        working-directory: common
        run: |
          cat >> "arch/arm64/configs/gki_defconfig" << 'EOF'
          CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y
          CONFIG_KALLSYMS=y
          CONFIG_TCP_CONG_ADVANCED=y
          CONFIG_TCP_CONG_BIC=n
          CONFIG_TCP_CONG_WESTWOOD=y
          CONFIG_TCP_CONG_HTCP=n
          CONFIG_TCP_CONG_BBR=y
          CONFIG_DEFAULT_WESTWOOD=y
          CONFIG_DEFAULT_TCP_CONG="westwood"
          CONFIG_OVERLAY_FS=y
          CONFIG_TMPFS_XATTR=y
          CONFIG_TMPFS_POSIX_ACL=y
          CONFIG_F2FS_FS=y
          CONFIG_F2FS_FS_SECURITY=y
          CONFIG_F2FS_FS_ENCRYPTION=y
          CONFIG_F2FS_FAULT_INJECTION=n
          CONFIG_F2FS_STAT_FS=y
          CONFIG_ZRAM=y
          CONFIG_ZRAM_DEF_COMP_LZ4=y
          CONFIG_ZRAM_DEF_COMP="lz4"
          CONFIG_ZRAM_MEMORY_TRACKING=n
          CONFIG_MEMCG=y
          CONFIG_MEMCG_SWAP=y
          CONFIG_LRU_GEN=y
          CONFIG_SCHED_AUTOGROUP=y
          CONFIG_SCHED_TUNE=y
          CONFIG_ENERGY_MODEL=y
          CONFIG_COMPACTION=y
          CONFIG_PAGE_COUNTER=y
          CONFIG_PSI=n
          CONFIG_PSI_DEFAULT_DISABLED=y
          CONFIG_MODULE_COMPRESS_NONE=y
          CONFIG_MODULE_SIG=n
          CONFIG_MODULE_FORCE_LOAD=n
          CONFIG_NET_SCHED=y
          CONFIG_NET_SCH_FQ_CODEL=y
          CONFIG_NET_SCH_FQ=y
          CONFIG_TCP_BBR2=y
          CONFIG_NET_NS=y
          CONFIG_HZ_1000=y
          CONFIG_HZ=1000
          # CONFIG_DEBUG_KERNEL is not set
          # CONFIG_TRACING_SUPPORT is not set
          # CONFIG_TRACING is not set
          # CONFIG_FTRACE is not set
          # CONFIG_TRACEPOINTS is not set
          # CONFIG_FUNCTION_TRACER is not set
          # CONFIG_FUNCTION_GRAPH_TRACER is not set
          # CONFIG_TRACE_PRINTK is not set
          # CONFIG_DYNAMIC_FTRACE is not set
          # CONFIG_DEBUG_INFO is not set
          # CONFIG_DEBUG_INFO_DWARF_TOOLCHAIN_DEFAULT is not set
          # CONFIG_DEBUG_INFO_DWARF4 is not set
          # CONFIG_DEBUG_INFO_DWARF5 is not set
          # CONFIG_DEBUG_INFO_BTF is not set
          # CONFIG_DEBUG_INFO_REDUCED is not set
          # CONFIG_DEBUG_INFO_COMPRESSED is not set
          # CONFIG_DEBUG_FS is not set
          # CONFIG_DYNAMIC_DEBUG is not set
          # CONFIG_DEBUG_PAGEALLOC is not set
          # CONFIG_PAGE_OWNER is not set
          # CONFIG_PAGE_POISONING is not set
          # CONFIG_SLUB_DEBUG is not set
          # CONFIG_SLUB_DEBUG_ON is not set
          # CONFIG_DEBUG_OBJECTS is not set
          # CONFIG_DEBUG_OBJECTS_TIMERS is not set
          # CONFIG_PROVE_LOCKING is not set
          # CONFIG_DEBUG_LOCKDEP is not set
          # CONFIG_DEBUG_SPINLOCK is not set
          # CONFIG_DEBUG_MUTEXES is not set
          # CONFIG_LOCK_STAT is not set
          # CONFIG_STACKTRACE is not set
          # CONFIG_DEBUG_STACK_USAGE is not set
          # CONFIG_DEBUG_STACKOVERFLOW is not set
          # CONFIG_RCU_TRACE is not set
          # CONFIG_RCU_EQS_DEBUG is not set
          # CONFIG_PRINTK_TIME is not set
          # CONFIG_PRINTK_CALLER is not set
          # CONFIG_PRINTK_INDEX is not set
          EOF
      
      - name: Build kernel
        run: |
          export KBUILD_BUILD_USER=yu
          export KBUILD_BUILD_HOST=github
          export SKIP_ABI_CHECKS=1
          export SKIP_CP_KERNEL_HDR=1
          
          BUILD_CONFIG=common/build.config.gki.aarch64 \
          LTO=thin \
          build/build.sh -j$(nproc) || exit 1
          
          cp out/android13-5.15/dist/Image ${{ matrix.variant.output }}
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.variant.name }}-kernel
          path: ${{ matrix.variant.output }}
          retention-days: 1
  
  package-release:
    name: "Package & Release"
    runs-on: ubuntu-22.04
    needs: [build-kernels]
    
    steps:
      - name: Setup environment
        run: |
          echo "BUILD_DATE=$(date +'%Y%m%d')" >> $GITHUB_ENV
          echo "TAG=v$(date +'%Y.%m.%d')" >> $GITHUB_ENV
      
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      
      - name: Package AIO kernel
        run: |
          git clone -q --depth=1 https://github.com/superuseryu/AnyKernel3 ak3
          
          cp artifacts/GKI-KSU-kernel/Image.ksu ak3/
          cp artifacts/GKI-NoKSU-kernel/Image.noksu ak3/
          
          ZIP_NAME="GKI-${{ env.DEVICE }}-AIO-${{ env.BUILD_DATE }}.zip"
          cd ak3
          zip -q -r9 "../$ZIP_NAME" * -x .git\* -x README.md
          cd ..
          
          ZIP_SIZE=$(stat -c%s "$ZIP_NAME" | awk '{printf "%.2f MB", $1/1024/1024}')
          ZIP_SHA256=$(sha256sum "$ZIP_NAME" | cut -d' ' -f1)
          
          echo "ZIP_NAME=$ZIP_NAME" >> $GITHUB_ENV
          echo "ZIP_SIZE=$ZIP_SIZE" >> $GITHUB_ENV
          echo "ZIP_SHA256=$ZIP_SHA256" >> $GITHUB_ENV
      
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG }}
          name: "Build #${{ github.run_number }}"
          body: |
            ## 📱 GKI Kernel AIO for ${{ env.DEVICE }}
            
            **Build #${{ github.run_number }} • Android 13-16**
            
            ### ✨ Features
            - 🔐 Wild-KSU + SUSFS v2.0.0 + BBG (KSU variant)
            - 🚀 TCP BBR + BBR2 + Westwood (default)
            - ⚡ Performance optimizations (LTO, HZ=1000)
            - 💾 ZRAM LZ4 + F2FS support
            - 🧩 MGLRU memory management
            - 🔇 Debug & tracing disabled
            - 📦 AIO: Both variants in one ZIP
            
            ### 📥 Installation
            1. Download `${{ env.ZIP_NAME }}`
            2. Flash via recovery (TWRP/OrangeFox)
            3. Choose: VOL+ (NoKSU) or VOL- (KSU)
            4. Reboot
            
            ### 🔐 Checksum
            ```
            SHA256: ${{ env.ZIP_SHA256 }}
            Size: ${{ env.ZIP_SIZE }}
            ```
            
            ### 📝 Notes
            - Compatible: Redmi Note 13 4G/NFC (sapphire/sapphiren)
            - TCP check: `cat /proc/sys/net/ipv4/tcp_congestion_control`
            - KSU Manager: [Download](https://github.com/WildKernels/Wild_KSU/releases)
          files: ${{ env.ZIP_NAME }}
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Send notification
        if: always()
        run: |
          TOKEN="${{ secrets.TELEGRAM_BOT_TOKEN }}"
          CHAT_ID="${{ secrets.TELEGRAM_CHAT_ID }}"
          
          if [ -z "$TOKEN" ] || [ -z "$CHAT_ID" ]; then
            exit 0
          fi
          
          RELEASE_URL="https://github.com/${{ github.repository }}/releases/tag/${{ env.TAG }}"
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          
          if [ "${{ job.status }}" == "success" ]; then
            SHORT_SHA=$(echo "${{ env.ZIP_SHA256 }}" | cut -c1-16)
            
            MESSAGE="<b>✅ Build Success!</b>%0A%0A"
            MESSAGE="${MESSAGE}<b>📦 File:</b> <code>${{ env.ZIP_NAME }}</code>%0A"
            MESSAGE="${MESSAGE}<b>📊 Size:</b> ${{ env.ZIP_SIZE }}%0A"
            MESSAGE="${MESSAGE}<b>🔐 SHA256:</b> <code>$SHORT_SHA...</code>%0A"
            MESSAGE="${MESSAGE}<b>🔗 Download:</b> <a href='$RELEASE_URL'>Release</a>%0A"
            MESSAGE="${MESSAGE}<b>👨‍💻 Maintainer:</b> @superuseryu%0A%0A"
            MESSAGE="${MESSAGE}#kernel #gki #wildksu #susfs #${{ env.DEVICE }}"
          else
            MESSAGE="<b>❌ Build Failed!</b>%0A%0A"
            MESSAGE="${MESSAGE}<b>🕐 Time:</b> $(date +'%H:%M UTC')%0A"
            MESSAGE="${MESSAGE}<b>🔗 Logs:</b> <a href='$RUN_URL'>View</a>"
          fi
          
          curl -s -X POST "https://api.telegram.org/bot$TOKEN/sendMessage" \
            -d chat_id="$CHAT_ID" \
            -d text="$MESSAGE" \
            -d parse_mode="HTML" \
            -d disable_web_page_preview="true" || true
            
