name: Build GKI Kernel (YASK Style)

on:
  workflow_dispatch:

jobs:
  build:
    name: "Build GKI Kernel"
    runs-on: ubuntu-latest

    steps:
      - name: "✨ Setup workspace"
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y bc bison flex libssl-dev make libelf-dev \
            rsync zip unzip curl git build-essential libncurses-dev \
            gawk wget diffutils

      - name: "🌟 Clone kernel source"
        run: |
          git clone https://android.googlesource.com/kernel/common \
            --branch android13-5.15-lts --depth=1 common

      - name: "📥 Initialize AOSP repo"
        run: |
          curl -o repo https://storage.googleapis.com/git-repo-downloads/repo
          chmod +x repo
          ./repo init -u https://android.googlesource.com/kernel/manifest -b common-android13-5.15-lts --depth=1
          ./repo sync -c -j$(nproc --all) --no-tags --no-clone-bundle
          rm -rf .repo

      - name: "🔧 Setup clang"
        run: |
          mkdir -p prebuilts/clang/host/linux-x86
          wget -q https://github.com/kdrag0n/proton-clang/archive/refs/heads/master.tar.gz -O clang.tar.gz
          tar -xzf clang.tar.gz -C prebuilts/clang/host/linux-x86
          mv prebuilts/clang/host/linux-x86/proton-clang-master prebuilts/clang/host/linux-x86/clang-proton
          rm clang.tar.gz

      - name: "🧩 Setup Wild-KSU"
        working-directory: ./common
        run: |
          curl -LSs "https://raw.githubusercontent.com/WildKernels/Wild_KSU/wild/kernel/setup.sh" | bash -s stable
          ln -sf Wild_KSU drivers/kernelsu

      - name: "🧩 Setup SUSFS"
        working-directory: ./common
        run: |
          git clone --depth=1 https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android13-5.15
          patch -p1 < susfs4ksu/kernel_patches/50_add_susfs_in_gki-android13-5.15.patch || true
          cp susfs4ksu/kernel_patches/fs/susfs.c fs/
          cp susfs4ksu/kernel_patches/include/linux/susfs*.h include/linux/
          sed -i '/#include <linux\/fs.h>/a #include <linux/susfs.h>' Wild_KSU/kernel/ksu.c
          sed -i '/ksu_core_init();/a \\tsusfs_init();' Wild_KSU/kernel/ksu.c
          rm -rf susfs4ksu

      - name: "🧩 Setup Baseband Guard"
        working-directory: ./common
        run: |
          git clone --depth=1 https://github.com/vc-teahouse/Baseband-guard.git
          ln -sf ../Baseband-guard security/baseband-guard
          echo 'obj-$(CONFIG_BBG) += baseband-guard/' >> security/Makefile
          sed -i '/^config LSM$/,/^help$/{ /^[[:space:]]*default/ { /baseband_guard/! s/selinux/selinux,baseband_guard/ } }' security/Kconfig
          sed -i '/source "security\/keys\/Kconfig"/a source "security/baseband-guard/Kconfig"' security/Kconfig

      - name: "⚙️ Configure kernel"
        working-directory: ./common
        run: |
          CONFIG_FILE="arch/arm64/configs/gki_defconfig"
          
          cat >> "$CONFIG_FILE" << 'EOF'
          CONFIG_KSU=y
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          CONFIG_KPROBE_EVENTS=y
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
          CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_SU=y
          CONFIG_BBG=y
          CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y
          CONFIG_TCP_CONG_ADVANCED=y
          CONFIG_TCP_CONG_BIC=n
          CONFIG_TCP_CONG_WESTWOOD=y
          CONFIG_TCP_CONG_HTCP=n
          CONFIG_TCP_CONG_BBR=y
          CONFIG_DEFAULT_WESTWOOD=y
          CONFIG_OVERLAY_FS=y
          CONFIG_TMPFS_XATTR=y
          CONFIG_TMPFS_POSIX_ACL=y
          EOF

      - name: "🔨 Build kernel"
        run: |
          LTO=thin \
          BUILD_CONFIG=common/build.config.gki.aarch64 \
          build/build.sh -j$(nproc --all)

      - name: "📦 Package kernel"
        run: |
          git clone --depth=1 https://github.com/superuseryu/AnyKernel3 ak3
          cp out/android13-5.15/dist/Image ak3/
          
          cd ak3
          zip -r9 "../GKI-Kernel-$(date +'%Y%m%d').zip" * -x .git\* -x README.md
          cd ..

      - name: "⬆️ Create Release"
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: GKI-Kernel-*.zip
          tag_name: "gki-kernel"
          name: "GKI Kernel"
          body: "GKI Kernel built"
